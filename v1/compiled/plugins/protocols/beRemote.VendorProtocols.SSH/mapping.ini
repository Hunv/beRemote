# DataType mapping
[settings.mapping.datatypes]
ssh.outputwrap=boolean
ssh.timeout=int32

# Key Value mapping for default values (e.g. if there are multiple values are needed)
[settings.mapping.definedvalues]

# The value specified here will add a suffix to the setting in the ui. e.g. if the setting has value 13 and suffix bit the ui will display "13 bit"
[settings.mapping.definedvalues.title]

# key mapping for human readable texts
[settings.mapping.displaytext]
ssh.outputwrap=Wrap returned text?
ssh.timeout=Request Timeout time (in ms)